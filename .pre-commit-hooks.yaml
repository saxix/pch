-   id: check-run-command
    name: check-run-command
    description: run shell command
    entry: check-run-command
    language: python
    pass_filenames: true

-   id: check-missed-migrations
    name: check-missed-migrations
    description: check for untracked django migrations
    entry: check-missed-migrations
    language: python
    pass_filenames: false
    always_run: true

-   id: check-untracked
    name: check-untracked
    description: check for untracked files
    entry: check-untracked
    language: python
    pass_filenames: false
    always_run: true
    minimum_pre_commit_version: 0.15.0


-   id: check-forbidden
    name: check-forbidden
    description: check for forbidden patterns
    entry: check-forbidden
    language: python
    pass_filenames: true
    always_run: true

-   id: check-minimized
    name: check-minimized
    description: check minimized version of *.js file is updated
    entry: check-minimized
    language: python
    pass_filenames: true
    always_run: true

-   id: check-tailwind
    name: check-tailwind
    description: check django-tailwind production version
    entry: check-tailwind
    language: python
    pass_filenames: true
    always_run: true
